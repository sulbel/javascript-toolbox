// find the length of a string
var myName = "StarBoy";
console.log(myName.length)

// indexing strings
var firstChar = myName[0];
console.log(firstChar)

// concatenate strings with + 
console.log(myName + " " + firstChar)